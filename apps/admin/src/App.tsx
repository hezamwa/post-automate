// Admin web dashboard (OD-17, design §15). Panels to build in Phase 2:
// monitor (spend/caps/health/runs) · AI routes CRUD + test · per-user limits ·
// global budget · user management · run explorer. Same Worker API, role=admin.
export function App() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>Post-Automate Admin</h1>
      <p>Phase 2 — see docs/design.md §15.</p>
    </main>
  );
}
